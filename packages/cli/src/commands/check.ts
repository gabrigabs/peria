/**
 * Check command stub - drift detection
 */

import { logger } from '../utils/logger.js'

export async function checkCommand(_cwd: string): Promise<void> {
  logger.info('Check command not implemented yet')
  logger.dim('Run "peria build" first to establish baseline')
}
